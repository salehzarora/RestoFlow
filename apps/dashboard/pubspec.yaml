name: restoflow_dashboard
description: RestoFlow dashboard app (RF-010 skeleton) - owner/manager dashboard app target. No features yet.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  restoflow_l10n:
    path: ../../packages/l10n
