name: restoflow_pos
description: RestoFlow pos app (RF-010 skeleton) - POS cashier app target. No features yet.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  restoflow_l10n:
    path: ../../packages/l10n
