import 'package:flutter/widgets.dart';

/// RestoFlow pos app - RF-010 skeleton entrypoint.
///
/// POS cashier app target. Placeholder only; real UI, routing, and features land in later tickets.
void main() => runApp(const SizedBox.shrink());
