<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- KIOSK-001: the kiosk talks to Supabase over the network in release too;
         flutter create grants INTERNET only in debug/profile. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- KIOSK-001-103: Bluetooth Classic (SPP) receipt printing — the same
         grants as POS/KDS (ANDROID-003). Android 12+ (API 31+) uses runtime
         BLUETOOTH_CONNECT (connect to a bonded printer) + BLUETOOTH_SCAN;
         "neverForLocation" declares scan does not derive location, so
         ACCESS_FINE_LOCATION is NOT needed (already-paired devices only, no
         in-app discovery). Older Android uses the legacy BLUETOOTH /
         BLUETOOTH_ADMIN (capped at API 30). Wi-Fi/TCP printing needs only
         INTERNET above. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <application
        android:label="BIZBOT Kiosk"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- PERF-112: production compatibility policy (owner-accepted). PowerVR
             hardware (Acer Iconia A16 / Allwinner A733 / PowerVR BXM) showed severe
             Impeller/Vulkan jank and ghosted/stale frames; the same-device PERF-111
             A/B with only this flag changed was "MUCH BETTER" on Skia/OpenGL.
             Keep Impeller disabled for POS/Kiosk until a verified Flutter engine
             upgrade resolves the affected device class (re-test on the Acer before
             removing). See docs/android-renderer-compatibility.md. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
