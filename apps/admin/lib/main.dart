import 'package:flutter/widgets.dart';

/// RestoFlow admin app - RF-010 skeleton entrypoint.
///
/// platform admin app target (isolated, audited path). Placeholder only; real UI, routing, and features land in later tickets.
void main() => runApp(const SizedBox.shrink());
