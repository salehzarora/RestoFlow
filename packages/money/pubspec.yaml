name: restoflow_money
description: RestoFlow money package (RF-036) - pure integer minor-unit money engine - money type, currency, rounding, discounts, and order totals (DECISION D-007).
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
resolution: workspace
dev_dependencies:
  test: ^1.25.0
