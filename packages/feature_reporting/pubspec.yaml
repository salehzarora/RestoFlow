name: restoflow_feature_reporting
description: RestoFlow feature_reporting package (RF-010 skeleton) - reporting feature package (composes domain + data + UI). No implementation yet.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
