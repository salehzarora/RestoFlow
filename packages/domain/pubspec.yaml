name: restoflow_domain
description: RestoFlow domain package (RF-010 skeleton) - entities, value objects, and the proposed state enumerations (D-018) - pure Dart, no Flutter, no IO. No implementation yet.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
resolution: workspace
