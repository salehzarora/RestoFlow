name: restoflow_domain
description: RestoFlow domain package - neutral entity/value-object foundations. Concrete entities and state enums land in later tickets; money type lives in packages/money (RF-036).
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
resolution: workspace
dev_dependencies:
  test: ^1.25.0
