name: restoflow_l10n
description: RestoFlow l10n package (RF-010 skeleton) - ar/he/en localization assets and bidi helpers. No implementation yet.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
