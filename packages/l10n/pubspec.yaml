name: restoflow_l10n
description: RestoFlow localization - shell only (declares supported locales). The full ar/he/en framework and RTL/LTR scaffolding is ticket RF-020.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
