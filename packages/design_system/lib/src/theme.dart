import 'package:flutter/material.dart';

/// Returns the RestoFlow base [ThemeData].
///
/// RF-011 shell only: this returns Flutter's default Material theme as a neutral
/// starting point. Brand colours, typography, spacing tokens, and RTL/LTR-aware
/// primitives (DECISION D-014) are added in later UI tickets.
ThemeData restoflowBaseTheme() => ThemeData();
