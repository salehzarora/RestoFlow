# OPEN_QUESTIONS.md — RestoFlow Open-Questions Register

> **Status — FROZEN: M0A architecture baseline, approved at RF-004.** Authored under RF-001, independently reviewed by Codex (RF-002), corrected under RF-003, and verified in a final Codex pass; the architecture freeze was **approved by the human owner, Saleh, at RF-004**. The explicit RF-001 invariants remain binding; decisions **D-001..D-028** are the frozen M0A baseline. Open questions **Q-001..Q-024** remain **Accepted Open** (per **DECISION D-027** — tracked, gating only their dependent tickets; none resolved or guessed). Changes to this frozen baseline now require the architecture-change procedure (a new ticket, independent review, and human approval). Any remaining inline pre-freeze status notes are superseded by this RF-004 approval. See [DECISIONS.md](DECISIONS.md) and [OPEN_QUESTIONS.md](OPEN_QUESTIONS.md).

**Status:** Frozen register, frozen as the M0A architecture baseline at RF-004 (approved into the frozen M0A baseline (RF-004)). This document is the single source of truth for open-question IDs (`Q-xxx`) cited across the RestoFlow documentation set. Per **DECISION D-015**, architecture documents in `docs/` are the source of truth for technical decisions and contracts; this file owns the open-questions register specifically.

**Scope of ownership:** Other documents (e.g. [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md), [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md), [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md), [PRINTERS_AND_HARDWARE_SPEC](PRINTERS_AND_HARDWARE_SPEC.md)) **cite** these IDs; they never invent conflicting ones. The decisions (frozen only after review and approval) live in [DECISIONS.md](DECISIONS.md) (`D-xxx`); the risk register lives where surfaced (`R-xxx`).

**Rule for new questions:** Q-018–Q-024 are now allocated (the OFFLINE_SYNC_SPEC O-S1..O-S7 items, promoted into this register). If any document needs an open question that is not listed here, it must be added to this register as **Q-025** or higher (next free number) and the citing document must reference the new ID. Do not reuse, renumber, or repurpose existing IDs. Do not silently convert an open question into an answer — a question is only closed when a matching **DECISION D-xxx** is recorded in [DECISIONS.md](DECISIONS.md) and this register is updated to `Status: Resolved → D-xxx`.

**Do not invent answers.** Legal, fiscal, tax, and jurisdiction questions (notably **Q-001**–**Q-005**) must be answered by the human owner (with qualified local advice), never guessed by an AI agent. Where a non-binding leaning exists it is explicitly marked **ASSUMPTION** and must not be treated as frozen.

---

## Blocker classification (read first) — DECISION D-027

Per **DECISION D-027** (PROPOSED, accepted for the RF-004 freeze candidate; pending Saleh's approval, **NOT** frozen), open questions are classified for blocking as follows:

- **RF-004 human architecture approval gates the START of [M0B](PROJECT_PLAN.md) as a milestone.** It does **not** require all of **Q-001..Q-024** to be resolved first.
- **An individual open question blocks ONLY the tickets that depend on its answer** — not a whole milestone, and not foundation/monorepo/CI work that does not touch the unanswered area.
- A new status, **"Accepted Open"**, applies to a question that may remain open without blocking the M0B milestone, provided **all four** conditions hold:
  1. its **owner is assigned**;
  2. its **blocking ticket / milestone is identified**;
  3. a **safe interim interface / config-point / placeholder / feature-flag exists** so dependent scaffolding can proceed; and
  4. **no irreversible schema or contract assumption** is made on the unanswered point.

This register covers **Q-001..Q-024**. RF-004 does **not** require all of them resolved; each unresolved question blocks only its dependent tickets per the **Blocking scope** column below.

---

## Early-resolution priority (read first)

A subset of these questions is high-priority for early engineering work and is tracked under ticket RF-003. These gate **specific tickets**, not all of M0B; per **DECISION D-027**, the start of the M0B milestone is gated by **RF-004 human architecture approval**, not by these questions. Each item below may be marked **"Accepted Open"** (owner assigned + safe-interim condition per D-027) rather than treated as a hard milestone blocker:

- **Q-001 — Target country / jurisdiction.** Gates legal/tax/fiscal/privacy work and the freeze of tax behaviour in [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md); it does **not** gate monorepo/CI/foundation tickets. May be **Accepted Open** with an owner while foundation work proceeds.
- **Q-007 — Default currency & single-vs-multi-currency.** Does **not** block generic money types using ISO 4217 currency codes (per **DECISION D-007**, money is integer minor units carrying a currency code); it gates the **default pilot currency configuration**. May be **Accepted Open** while the currency-agnostic money type is scaffolded.
- **Q-006 / Q-015 — Pilot hardware & printing connectivity/encoding.** Gate pilot hardware selection + M3 verification and printer implementation/goldens (Q-015); they do **not** gate the abstract printing **adapter interface** (de-risks Arabic/Hebrew printing — **RISK R-001**, **RISK R-006**). May be **Accepted Open** behind a replaceable printing adapter.
- **Q-008 — MFA method and mandatory roles.** Gates the concrete MFA provider/enforcement implementation (**DECISION D-006**); it does **not** gate the identity **interfaces**. May be **Accepted Open** behind those interfaces.
- **Q-009 — Offline authorization validity window.** Gates final revocation/session hardening (**DECISION D-010**; **RISK R-007**); it does **not** gate Drift outbox/inbox **local-store scaffolding**. May be **Accepted Open** with a placeholder/config-point window.

Per **DECISION D-027**, every question in the register is recorded as **Accepted Open** and blocks only its dependent tickets, not whole milestones. Nothing here blocks **M0A** (this milestone is documentation-only); the M0B milestone start is gated by **RF-004** approval, not by the items above.

---

## Register

> Every row below is **Accepted Open** (per **DECISION D-027**): the question is **unresolved** but, because it blocks only its dependent tickets (the **Blocking scope** column) and a safe interim interface/config-point/placeholder/feature-flag exists with no irreversible schema/contract assumption, it does **not** gate the M0B milestone (RF-004 approval does). No row here is `Resolved` — none is closed by a recorded decision. **"Accepted Open" never answers the question.** For the legal/fiscal/privacy questions (**Q-001..Q-005, Q-012**) it is a **gate classification only, never a legal answer**: those remain unresolved and **must not be guessed** by any AI agent — they are decided by the **Human owner** with qualified local advice, block only their legal/fiscal/privacy-specific dependent tickets, and unrelated foundation work proceeds behind generic interfaces/placeholders. "Owner" names who must decide; "Leaning" is a non-binding **ASSUMPTION** only where one is given; absence of a leaning is deliberate.
>
> **Status vocabulary (legend):** **Open** (no decision yet) · **Accepted Open** (per **DECISION D-027** — open but non-blocking for the M0B milestone when its owner is assigned, its blocking ticket/milestone is identified, a safe interim interface/config-point/placeholder/feature-flag exists, and no irreversible schema/contract assumption is made) · **Resolved → D-xxx** (closed by a recorded decision). Per **DECISION D-027**, the **Blocking scope** column lists the dependent tickets/work a question blocks; an open question does **not** block a whole milestone, and **RF-004 does not require all Q-001..Q-024 resolved**.

| ID | Question | Why it matters / what it blocks | Owner | Blocking scope (per D-027) | Status | Proposed default / leaning |
|----|----------|---------------------------------|-------|----------------------------|--------|----------------------------|
| **Q-001** | What is the initial target country / jurisdiction for the pilot and first sellable release? | Determines tax model, fiscal receipt/invoice law, data-privacy obligations, currency, and language emphasis. Blocks the freeze of money/tax behaviour in [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md). **RISK R-008**. | Human (legal/business) | Blocks legal/tax/fiscal/privacy work + M2 money/tax freeze; does **not** block monorepo/CI/foundation tickets (**D-027**) | **Accepted Open** | None — **do not guess legal jurisdiction.** |
| **Q-002** | What VAT/sales-tax rules and rates apply, and is the default **tax-inclusive** or **tax-exclusive** pricing? | Drives tax computation, rounding order, and how item/modifier price snapshots (**DECISION D-008**) relate to displayed totals. Owned by [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md). Depends on Q-001. **RISK R-008**. | Human (legal/finance) | Blocks final tax calculation + fiscal implementation tickets (**D-027**) | **Accepted Open** | None until Q-001 resolved. Money stays integer minor units regardless (**DECISION D-007**). |
| **Q-003** | What are the fiscal receipt/invoice legal requirements, and is certified fiscal hardware/software mandated? | May require certified printers, signed fiscal records, or government reporting; could constrain [PRINTERS_AND_HARDWARE_SPEC](PRINTERS_AND_HARDWARE_SPEC.md) and the receipt pipeline. Depends on Q-001. | Human (legal) | Blocks fiscal receipt integration tickets (M3 pilot legality; may affect M2) (**D-027**) | **Accepted Open** | None — legal. |
| **Q-004** | What legal rules govern receipt/invoice numbering (format, mandatory sequence, reset cadence)? | Constrains the per-branch monotonic server-assigned receipt sequence (**DECISION D-021**); some jurisdictions mandate format/annual reset. Owned by [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md). Depends on Q-001. | Human (legal) | Blocks final fiscal numbering rules; a generic receipt-sequence interface may still be designed (**D-027**) | **Accepted Open** | None — legal. D-021 mechanism stands; legal format layered on top. |
| **Q-005** | What data-retention and privacy obligations apply (retention periods, subject rights, residency)? | Affects audit-event retention (**DECISION D-013**), backups (Q-013), tombstone/soft-delete semantics (**DECISION D-020**), and DR region. Owned by [OPERATIONS_AND_RECOVERY](OPERATIONS_AND_RECOVERY.md). Depends on Q-001. | Human (legal/ops) | Blocks M4 production hardening; informs M2 | **Accepted Open** | None — legal/privacy. |
| **Q-006** | Which specific pilot hardware (printer model(s), POS tablet, KDS display, cash drawer) will be used? | Standardizing one printer model is the primary mitigation for ESC/POS variation (**RISK R-001**); drives the [PILOT_PLAN](PILOT_PLAN.md) and [PRINTERS_AND_HARDWARE_SPEC](PRINTERS_AND_HARDWARE_SPEC.md). | Human (owner/ops) | Blocks pilot hardware selection + M3 verification; **Accepted Open** behind a replaceable printing adapter (**D-027**) | **Accepted Open** | **ASSUMPTION:** single standardized ESC/POS network printer + one Android tablet for POS and one for KDS; confirm at pilot. |
| **Q-007** | What is the default currency, and must a single order ever support multiple currencies? | Per **DECISION D-007** money is integer minor units with currency per organization (overridable per restaurant); single currency per order. Migrations in M0B need correct currency semantics. | Human + ChatGPT (planning) | Blocks **default pilot currency configuration**; does **not** block generic money types using ISO 4217 currency codes (**D-027**) | **Accepted Open** | **ASSUMPTION:** single currency per order (proposed, approved into the frozen M0A baseline (RF-004)); one ISO 4217 currency per organization for the pilot. Multi-currency per order is **DEFERRED**. |
| **Q-008** | Which MFA method(s) (TOTP / SMS / email) ship, and for which roles is MFA mandatory? | **DECISION D-006** requires MFA for privileged/sensitive roles. Needed before the auth foundation and RLS skeleton. Owned by [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md). | Human + ChatGPT (planning) | Blocks concrete MFA provider/enforcement implementation (enforced M2); does **not** block the identity interfaces — **Accepted Open** behind them (**D-027**) | **Accepted Open** | **ASSUMPTION:** TOTP mandatory for `platform_admin`, `org_owner`, `restaurant_owner`; optional for `manager`; not applicable to PIN-only `cashier`/`kitchen_staff` sessions. |
| **Q-009** | How long are cached permissions / a human PIN session valid while a device is offline? | Bounds the window in which a revoked employee/device can still act offline (**RISK R-007**); server must reject stale ops on reconnect. Owned by [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) / [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md). | Human + ChatGPT (planning) | Blocks final revocation/session hardening (enforced M2); does **not** block Drift local-store scaffolding — **Accepted Open** with a placeholder/config-point window (**D-027**) | **Accepted Open** | **ASSUMPTION:** short window (leaning ≤ 8–24h cached authorization; PIN idle timeout minutes-scale) — confirm; server reconnect re-validation is mandatory regardless. |
| **Q-010** | What is the conflict-resolution policy per entity (last-write-wins vs domain-specific rules)? | Defines multi-device conflict handling in the outbox/inbox model (**DECISION D-010**); orders/payments need domain rules, not naive LWW (**RISK R-002**). Owned by [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md). | ChatGPT + Human | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** domain-specific rules for orders/payments/receipt-number; LWW only for low-risk descriptive fields. Per-entity table to be authored in OFFLINE_SYNC_SPEC. |
| **Q-011** | How will tips be modelled (capture, distribution, taxation) in a future release? | Tips are **DEFERRED** for MVP but flagged so money/state models do not preclude them; touches [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md). | Human + ChatGPT | DEFERRED (post-MVP) | **Accepted Open** | **DEFERRED.** Keep payment/money schema extensible; no tips logic in MVP. |
| **Q-012** | What are the service-charge rules, and is service charge taxable? | Affects order totals and tax base; interacts with Q-002. Owned by [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md). Depends on Q-001/Q-002. | Human (legal/finance) | Blocks M2 money freeze (if in MVP) | **Accepted Open** | None until Q-001/Q-002 resolved. Likely **DEFERRED** for pilot unless required. |
| **Q-013** | What are the backup RPO/RTO targets and the disaster-recovery region? | Drives backup cadence, DR strategy, and residency (interacts with Q-005). Owned by [OPERATIONS_AND_RECOVERY](OPERATIONS_AND_RECOVERY.md). | Human (ops) | Blocks M4 production hardening | **Accepted Open** | **ASSUMPTION:** RPO ≤ 24h, RTO ≤ 24h for pilot; tighten before M4. DR region tied to Q-001 residency. |
| **Q-014** | What are the Supabase Realtime provider limits, and what is the fallback polling interval? | Realtime is an **enhancement only** (**DECISION D-010**); the system must degrade to polling/outbox. Quantifies limits and fallback. Owned by [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) / [ARCHITECTURE](ARCHITECTURE.md). **RISK** noted in register (Realtime limits). | ChatGPT + Claude Code | Blocks M2 sync (enhancement layer) | **Accepted Open** | **ASSUMPTION:** fallback polling interval in the seconds-to-low-tens-of-seconds range; outbox/inbox remains source of truth regardless. |
| **Q-015** | What is the pilot printing connectivity (network / USB / Bluetooth) and the Arabic/Hebrew encoding/raster strategy? | Encoding correctness is a known risk (**RISK R-006**); raster fallback is the mitigation. Connectivity affects the adapter and pilot setup. Owned by [PRINTERS_AND_HARDWARE_SPEC](PRINTERS_AND_HARDWARE_SPEC.md). Tied to Q-006. | Human + Claude Code | Blocks printer implementation/goldens + pilot selection (M3); does **not** block the abstract printing adapter interface — **Accepted Open** behind it (**D-027**) | **Accepted Open** | **ASSUMPTION:** network (TCP) connectivity; **raster/image rendering** for Arabic/Hebrew to avoid codepage gaps, validated in pilot. |
| **Q-016** | What is the subscription / billing model for M4 (plans, metering, payment provider)? | Needed for the sellable SaaS milestone; flagged now so multi-tenant schema (org-scoped) does not preclude billing. Owned by [PRODUCT_SPEC](PRODUCT_SPEC.md) / [PROJECT_PLAN](PROJECT_PLAN.md). | Human (business) | DEFERRED to M4 | **Accepted Open** | **DEFERRED.** Billing is org-scoped when introduced; no billing logic before M4. |
| **Q-017** | Does the `accountant` (read-only) role ship in the MVP, or later? | Affects the role set and RLS/permission tests (**DECISION D-012**). Role key `accountant` is reserved either way. Owned by [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md) / [MVP_SCOPE](MVP_SCOPE.md). | Human + ChatGPT | Blocks M2/M4 scope finalization | **Accepted Open** | **ASSUMPTION:** reserve the `accountant` role key now; full read-only reporting access likely **DEFERRED** to post-pilot. |
| **Q-018** | What are the concrete retry/backoff constants (base delay, max delay, jitter, max attempts) for replaying queued offline operations? | Determines how aggressively a reconnecting device drains its outbox without stampeding the server; wrong values cause thundering-herd or stale-op storms. Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S1. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** exponential backoff with jitter (e.g. base ~1s, cap ~tens of seconds, bounded max attempts before poison handling); confirm in OFFLINE_SYNC_SPEC. |
| **Q-019** | What are the retention windows for the outbox and the processed-pull-log (how long applied/acknowledged entries are kept before pruning)? | Bounds local-storage growth and the dedup/idempotency window on device and server; too short risks replay gaps, too long bloats storage. Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S2. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** retain processed entries long enough to cover the max offline window plus margin; prune after confirmed server acknowledgment. Confirm values. |
| **Q-020** | What tooling and operator handling exist for poison / dead operations (ops that repeatedly fail to apply)? | Without a dead-letter path and operator visibility, a single bad op can wedge a device's outbox indefinitely. Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S3. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** move ops past the max-attempts threshold to a quarantined dead-letter queue surfaced to support; never silently drop. Confirm tooling. |
| **Q-021** | What is the canonical reconciliation order on reconnect — is pull-then-push ratified? | Ordering affects conflict detection and idempotency; pull-then-push lets the device see server state before replaying local ops. Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S4. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** ratify **pull-then-push** (apply server changes first, then replay outbox). Confirm in OFFLINE_SYNC_SPEC. |
| **Q-022** | When the offline-authorization window (Q-009) is exceeded, does the device hard-stop, or queue-and-reject on reconnect? | Defines fail-safe behaviour for an over-long offline session; balances continuity of service against acting on stale authorization (**RISK R-007**). Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S5. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** server-side reconnect re-validation is mandatory regardless; leaning toward queue-and-reject of stale-authorization ops over a hard client lock. Confirm. |
| **Q-023** | What is the maximum offline outbox depth / local-storage ceiling, and how does the device behave when it is reached? | Prevents unbounded local growth during long outages; the chosen behaviour (block new ops vs. degrade) affects M1 local UX and M2 sync. Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S6. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets + M1 local-storage-behaviour ticket, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** define a bounded ceiling with graceful degradation and operator warning before the limit; no silent data loss. Confirm value/behaviour. |
| **Q-024** | How is clock skew between devices handled for last-writer-wins tie-breaks? | LWW on descriptive fields (Q-010) depends on comparable timestamps; unmanaged skew makes tie-breaks non-deterministic. Promoted from [OFFLINE_SYNC_SPEC](OFFLINE_SYNC_SPEC.md) O-S7. | Claude Code proposes / Human + ChatGPT decide | Blocks the relevant M2 sync-hardening tickets, **not** all of M0B (**D-027**) | **Accepted Open** | **ASSUMPTION:** prefer server-assigned timestamps / server-side ordering for tie-breaks rather than trusting device clocks; bound acceptable skew. Confirm. |
| **Q-025** | How is tenant menu content localized for the public Storefront (per-locale names / descriptions for ar / he / en), given that the catalog stores one name per row today? | The live read (**DECISION D-039**) serves the stored name verbatim under every locale root; the UI chrome is localized, the tenant's own text is not. Affects the menu schema, the Dashboard editor and the served envelope. Owned by [DOMAIN_MODEL](DOMAIN_MODEL.md) / [PRODUCT_SPEC](PRODUCT_SPEC.md). | Human + ChatGPT (planning) | Blocks a future Storefront localization ticket only; does **not** block READ-001 (raised STOREFRONT-READ-001, 2026-09-23) | **Open** | **ASSUMPTION:** one stored name per row, rendered as-is under every root, until a per-locale field set is decided; no automatic translation. |
| **Q-026** | What is the delivery model for the Storefront (zones, fees, minimums, radius) and where does it live? | `delivery_enabled` is served as the literal `false` and the zone list is fixture-only; the read-presentation fields exist but no backend does. Owned by [PRODUCT_SPEC](PRODUCT_SPEC.md) / [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md) (fees are money). | Human (business) + ChatGPT | Blocks the future delivery ticket only; browse-only READ-001 is unaffected (raised STOREFRONT-READ-001, 2026-09-23) | **Open** | **DEFERRED.** Keep `delivery_enabled=false` forced by CHECK until a zone / fee model is approved; no delivery pricing in the public read. |
| **Q-027** | Is the 60 s ISR window (plus 300 s stale-while-revalidate) acceptable for sold-out flips and hour changes, or is on-demand invalidation (per-slug revalidation on publish / availability change) required? | Freshness bound of every public document (**DECISION D-039** point 6); on-demand invalidation needs a server-side trigger path from the Dashboard / POS into the site host. Owned by [ARCHITECTURE](ARCHITECTURE.md). | Claude Code proposes / Human decides | Blocks a future freshness ticket only (raised STOREFRONT-READ-001, 2026-09-23) | **Open** | **ASSUMPTION:** 60 s + 300 s is acceptable for a browse-only menu; revisit before ordering goes live. |
| **Q-028** | What is the hosted role-level `statement_timeout` of `anon`, and is a DB-side read throttle / provider rate limit required for the public read? | The only execution bound of `public.storefront_menu` is the `anon` role config (a hosted read the repository does not record); no DB-side read throttle exists, so direct anon-key callers can generate compute / egress at will (public data). Owned by [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md) / [OPERATIONS_AND_RECOVERY](OPERATIONS_AND_RECOVERY.md). | Human (ops) + Claude Code | Blocks the hosted apply of the READ-001 migration until the value is **read** ([DEPLOYMENT](DEPLOYMENT.md) §16 query 11); a throttle is a separate ticket (raised STOREFRONT-READ-001, 2026-09-23) | **Open** | **ASSUMPTION:** read the value first; propose `ALTER ROLE anon SET statement_timeout` only as its own approved change if none exists; rely on the CDN + spend / egress alerts for abuse. |
| **Q-029** | May a published slug change, and if so with what history (308 redirects, reserved old slugs, cart migration)? | The slug is immutable in READ-001 (**DECISION D-039**, plan decision D4); a rename breaks printed / shared links and the per-slug local cart. The writer refuses a rename with `slug_immutable`; its `slug_taken` check answers for every live row of every tenant, so a manager can probe slug availability (inherent to a global slug namespace; no audit row today). Owned by [PRODUCT_SPEC](PRODUCT_SPEC.md) / [DOMAIN_MODEL](DOMAIN_MODEL.md). | Human (business) + ChatGPT | Blocks a future slug-management ticket only (raised STOREFRONT-READ-001, 2026-09-23) | **Open** | **ASSUMPTION:** immutable once set; a change, if ever allowed, keeps the old slug reserved and redirected. |
| **Q-030** | How are tax-**inclusive** tenants and non-ILS currencies presented on the public Storefront? | READ-001 publishes only `ILS` + exclusive-tax tenants (plan decisions D7 / D7b); an inclusive-tax or non-ILS tenant is `not_found` on the public read and blocked from publishing. Interacts with Q-001 / Q-002. Owned by [MONEY_AND_TAX_SPEC](MONEY_AND_TAX_SPEC.md). | Human (finance) + ChatGPT | Blocks publishing such tenants only; the pilot tenant is ILS / exclusive (raised STOREFRONT-READ-001, 2026-09-23) | **Open** | **ASSUMPTION:** pilot = ILS + exclusive tax shown as a separate line at the configured `rate_bp`; inclusive display decided with Q-002. |
| **Q-031** | Is committing and deploying the vendored `magick.wasm` — which statically links LGPL components (glib, libraw, liblqr, libheif, libde265) — "distribution" that triggers LGPL obligations? | The first implementation (recipe c3) vendored `@imagemagick/magick-wasm` 0.0.43 into the `storefront-media-publish` Edge Function; its recipe never reached the LGPL parts, but they are present in the binary, and the repository is **public**, so a first push would have published that object code (independent review LIC-1, a BLOCKER). **Answered for the publication branch by replacement (DECISION D-040, owner packet "Q031 REMEDIATION + CORRECTION PASS", 2026-09-25):** the v2 branch `feat/STOREFRONT-PUBLISH-001-v2-focused-codecs` was created from `main` `6cf6c20f`, has never contained the ImageMagick wasm (no path of it anywhere in its history), and vendors instead ten jSquash codec files whose licence inventory — of the actual binaries — found **no copyleft component**; the notices ship inside every deploy bundle and are hash-pinned with the codecs (`supabase/functions/storefront-media-publish/vendor/README.md`, `test/license.test.mjs`). That is an engineering compliance package, not a declaration of legal sufficiency. The evidence branch `feat/STOREFRONT-PUBLISH-001-profile-editor-media` (head `3e0c3ccb`) still carries the ImageMagick wasm in its unpushed history: it must **never** be pushed. Owned by [DECISIONS](DECISIONS.md) (D-040). | Human (legal / business) | The owner's approval to publish the v2 branch is a hard step before any push or hosted canary ([DEPLOYMENT](DEPLOYMENT.md) §17.1 step 0); pushing the evidence branch is never allowed; local work is unaffected (raised STOREFRONT-PUBLISH-001, 2026-09-25; answered by replacement the same day) | **Open** — answered for the publication branch by replacement under PROPOSED **D-040**; not `Resolved` until D-040 is ratified | **ASSUMPTION:** for the permissive codecs, shipping `THIRD_PARTY_NOTICES.txt` byte-exact with every bundle and keeping the IJG statement in the accompanying documentation (`vendor/README.md`) meets the attribution terms; the owner confirms (with legal advice where needed) before publication, and any future vendored codec re-runs the licence inventory first (the `vendor/README.md` upgrade rule). |
| **Q-032** | How and when are retracted, orphaned and unwanted `storefront-media` objects removed? | No path deletes a `storefront-media` object (**DECISION D-040**). A RETRACTED derivative is no longer referenced by the public read, but its object stays fetchable by anyone who holds its URL, and every derivative is uploaded with `cache-control: max-age=31536000` — a one-year cache lifetime not tied to retraction, so a CDN or browser copy can outlive any removal. A cancelled STAGED row leaves its uploaded object **unregistered**, and the registration-keyed storage policies then deny the tenant any write to it, so the tenant cannot remove it at all; a registered object (STAGED, LIVE or RETRACTED) can be deleted only through the raw Storage API, not through the product. **A removal request (for example an employee's photo used as the hero) therefore cannot be honoured by the tenant through the product today; it needs an operator path.** A suspended tenant's objects also stay public (review STG-5). Storage cost and removal requests need a purge; interacts with Q-005 (retention) and is coupled with Q-033 (narrowing the storage policies would also remove the raw-API delete). Owned by [DOMAIN_MODEL](DOMAIN_MODEL.md) §4.8 / [OPERATIONS_AND_RECOVERY](OPERATIONS_AND_RECOVERY.md). | Human (ops) + Claude Code | Blocks a future purge ticket; the owner decides whether the first real-tenant image publication on hosted waits for it or for an operator removal runbook (a privileged, audited delete of a named object) (raised STOREFRONT-PUBLISH-001, 2026-09-25; widened by the correction pass, reviews STG-4 / STG-5) | **Open** | **ASSUMPTION:** keep every object until an approved ticket, designed together with Q-033, adds a privileged, audited, state-aware removal (unregistered objects, RETRACTED rows past a retention window, a named object on request); consider a shorter `max-age` for derivatives, and have the Retract confirmation say that the file stays reachable by its link. |
| **Q-033** | Should `storefront-media` storage writes be gated by the media row's state (for example INSERT only for a STAGED key) instead of by registration alone? | The four READ-001 storage policies are **registration-scoped, not state-scoped**: a manager+ of a restaurant may insert, overwrite or delete the object of **any registered key of that restaurant**, whatever the row's state. No policy grants cross-tenant access (a cashier, a sibling-restaurant manager, another tenant and `anon` cannot write; object prefixes are bound to the restaurant). The concrete impact inside one restaurant (review STG-2): a manager can overwrite or delete the LIVE, in-use logo or hero behind the owner's back **without an `audit_events` row** (raw Storage writes are not audited — the gap T-014 records for `menu-images`), while `storefront_menu` keeps serving the URL and `list_storefront_media` keeps listing the row as `published`; an overwritten content address makes every later publish of those bytes answer `409 object_conflict` until a manager deletes the object through the raw Storage API; and the `finalize` object check is point-in-time. The storefront site's deployment protection does not bound this: the objects are served by Storage at public URLs. Narrowing the policies changes what T-017 pins. Owned by [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md) §3 Layer 3 / [API_CONTRACT](API_CONTRACT.md) §4.42.4. | Claude Code proposes / Human decides | Blocks a future storage-hardening ticket only (raised STOREFRONT-PUBLISH-001, 2026-09-25; risk text corrected by the correction pass) | **Open** | **ASSUMPTION:** accept the one-restaurant residual for now (managers+ of that restaurant only; no cross-tenant path); a later ticket, designed together with Q-032, narrows INSERT to STAGED keys, drops UPDATE (content-addressed objects never need replacing), routes deletion through the audited removal path of Q-032 and amends T-017 — which on its own does not close Q-036. |
| **Q-034** | Where is the READ-001 opening-hours validator's presence defect fixed — folded into the STOREFRONT-PUBLISH-001 migration, or under its own Work ID? | `app.storefront_opening_hours_is_valid` accepts weekly / exception entries with **missing** keys (`{"weekly":[{}]}`, `{"exceptions":[{}]}`, `{"weekly":[{"dow":1}]}` and `{"weekly":[{"dow":1,"open":"09:00"}]}` all return true) because a NULL condition in a plpgsql `IF` is false; `app.storefront_publish_blockers` counts any weekly entry as a window, so a manager calling the profile RPC directly can publish a storefront whose hours never open (it shows closed permanently) or — with an entry that has no close — one whose public read announces `next_open` at the next Monday 09:00 forever and never opens. **The STOREFRONT-PUBLISH-001 editor cannot create this state** (it only sends complete `{dow, open, close}` entries), and after the correction pass **it refuses to publish over it**: saved hours it cannot read are kept exactly as stored, shown as an error, lock the hours editor until an explicit repair, and add the client-side blocker `hours_unreadable`, so Publish stays disabled and "all requirements met" is never shown (review OQ-1; [API_CONTRACT](API_CONTRACT.md) §4.44). The READ-001 validator is deliberately left untouched: the packets keep the READ-001 routines unchanged, and the function backs the `opening_hours` CHECK, so a stricter version needs a count of stored rows it would reject first. Owned by [DOMAIN_MODEL](DOMAIN_MODEL.md) §4.7 / [API_CONTRACT](API_CONTRACT.md) §4.42.2. | Human decides / Claude Code implements | Does not block the Dashboard path; blocks the hours contract's integrity for direct callers until fixed (raised STOREFRONT-PUBLISH-001, 2026-09-25) | **Open** | **ASSUMPTION:** a small dedicated fix — every key required, a pgTAP regression per missing-key shape (the no-close shape included), and a read-only PRE count of stored rows the stricter check would reject. |
| **Q-035** | Should the storefront publish blockers (or a "served" flag) report every condition under which the public read does not serve a published profile — a suspended or soft-deleted organization, restaurant or branch, and a catalog over the `payload_limit` caps? | The public read (`public.storefront_menu`, READ-001) answers `not_found` when the organization, restaurant or storefront branch is not `active` or is soft-deleted, and a typed `payload_limit` when the live catalog exceeds 100 categories / 500 items / 2,000 modifiers / 8,000 options ([API_CONTRACT](API_CONTRACT.md) §4.42.1), but `app.storefront_publish_blockers` checks only the slug, the branch, the time zone, the currency, the tax mode, a live item and weekly hours, so a published profile in any of those states reports no blocker. `payload_limit` is reachable through ordinary menu growth (review OQ-3). After the correction pass the Dashboard card never claims "public": a published profile with no blockers reads "Published." with a note that this does not guarantee visitors can open the page, and a suspended branch is marked in the branch picker ([API_CONTRACT](API_CONTRACT.md) §4.44). Separately, the media RPCs and the storage policies authorize through `app.actor_rank_in_scope`, which checks membership status only, so a suspended tenant's manager can still stage, upload and publish into the public bucket (review STG-5). Changing the derivation changes a READ-001 routine, which the packets keep unchanged. Owned by [API_CONTRACT](API_CONTRACT.md) §4.42.2 / §4.44. | Human decides / Claude Code implements | UX follow-up; blocks nothing today: the public page fails closed and keeps its anti-oracle, and only the card's status can be incomplete. No platform-admin path writes `status`; a tenant owner (rank ≥ 3 over the scope) can set `suspended` through `public.update_organization_settings` / `update_restaurant_settings` / `update_branch_settings` (`p_status`, RF-112), which the Dashboard Settings card calls with the prefilled status and no suspend control — so today only a direct RPC call or an operator's SQL reaches a suspended state. A future platform-suspend control must land this first and also gate the media write path and decide what happens to the tenant's public objects (Q-032) (raised STOREFRONT-PUBLISH-001, 2026-09-25; widened by the correction pass) | **Open** | **ASSUMPTION:** a small dedicated change adds `tenant_inactive` (per level) and `payload_limit` blockers to the derivation — or a `served` flag computed from the `storefront_menu` predicates in `get_restaurant_storefront_profile` — with a pgTAP case per level and the Dashboard labels in ar / he / en. |
| **Q-036** | Should the database — not only the Edge Function — ensure that a published `storefront-media` object is the canonical derivative, or is manager trust for the bytes of one's own storefront accepted? | The canonical recipe (re-encode, metadata strip, the W × W box, ≤ 512 KiB, content address = sha-256 of the bytes, never a private original) is enforced by the Edge Function, the honest client (**DECISION D-040**). The database cannot tell the function from any manager's JWT: `stage` takes the content hash, width, height and bytes as the caller's declaration, and `finalize` checks only that an object **row** exists under the registered key whose **declared** metadata says `image/webp` and the staged size — it never reads the bytes (reviews DB-1 / DB-2). So any manager+ of a restaurant — including the owner of a self-serve organization (`public.create_organization` is granted to `authenticated`) — can stage fabricated facts, upload arbitrary bytes declared `image/webp` through the raw Storage API and finalize them onto **their own** storefront (for example an unstripped original with its EXIF / GPS data). Per review DB-1 (verified on the local stack's storage-api v1.60.20; the hosted version was not checked), the Storage API's copy route can carry another object's metadata past the bucket's size and type limits, so such an object can exceed 512 KiB (up to the source bucket's cap) or not be WebP. An uploaded object is publicly fetchable from the moment of upload, before any `finalize`, and a stage → upload → cancel sequence leaves an orphan the tenant cannot remove (Q-032; review STG-1). Nothing here crosses tenants: object prefixes are bound to the restaurant, the storage policies admit only registered keys of a restaurant the caller manages, and `stage` / `finalize` write audit rows naming the actor and the declared hash. The migration's header records this residual. Owned by [SECURITY_AND_THREAT_MODEL](SECURITY_AND_THREAT_MODEL.md) §3 Layer 3. | Claude Code proposes / Human decides | Blocks no local work; the independent review recommends the owner's explicit acceptance of this residual — or hardening — before the migration and the function are deployed to hosted while self-serve signup is open (raised by the STOREFRONT-PUBLISH-001 correction pass, 2026-09-25) | **Open** | **ASSUMPTION:** accept manager trust for the bytes of one's own storefront for now (the actor is audited and confined to their tenant); hardening options, each its own Work ID: record the recipe id and rung at `stage` so an audit can re-derive and compare; re-hash the object server-side in `finalize`, or accept only a function-issued proof; narrow the storage policies (Q-033, which alone does not close this path); a per-restaurant object quota or rate limit; check the Storage copy / move behaviour on hosted. |
| **Q-037** | Should the storefront pause-end editor take its input in the storefront **branch's** IANA time zone instead of the managing device's local time zone? | The STOREFRONT-PUBLISH-001 v1 picker interprets the entered wall time in the managing device's local time zone, labels it with the UTC offset that the device's time-zone rules give for the chosen pause instant (the device's current offset while no pause end is chosen) and stores an absolute `timestamptz` instant (**DECISION D-040** point 10; [DOMAIN_MODEL](DOMAIN_MODEL.md) §4.7). The owner accepted this for v1 on 2026-09-25: no time-zone database dependency and no migration change in that ticket. A manager whose device is in another zone than the branch enters a different instant than they may intend; the offset label makes the zone visible but does not convert. Owned by [DOMAIN_MODEL](DOMAIN_MODEL.md) §4.7 / [API_CONTRACT](API_CONTRACT.md) §4.44. | Claude Code proposes / Human decides | Blocks a broader multi-time-zone rollout of the storefront editor (raised STOREFRONT-PUBLISH-001 final correction pass, 2026-09-25) | **Open** | **ASSUMPTION:** v1 stays device-local; a later Work ID adds branch-zone entry (and decides the time-zone database dependency) before managers in other zones than their branches use the editor. |
| **Q-038** | Should the database validator or the public read canonicalize **touching** opening-hours windows (one closes at the minute the next opens, also across midnight), for values written through the RPC directly or before the Dashboard check? | `app.storefront_opening_hours_is_valid` accepts touching windows, and `app.storefront_service_window` announces the closing time of one window (the earliest-starting one containing the moment), so 09:00–12:00 + 12:00–23:00 shows "closes 12:00" although the restaurant stays open. The STOREFRONT-PUBLISH-001 Dashboard closes the **weekly-window** path only: its weekly editor refuses to save touching weekly windows, asks the manager to merge them (never merging them itself) and blocks Publish for saved ones (`hours_touching`; [API_CONTRACT](API_CONTRACT.md) §4.44). **Still open (accepted follow-ups of that ticket):** the date-exception editor can create touching boundaries — a weekly Monday 18:00–00:00 with a Tuesday exception 00:00–03:00, or two consecutive exception dates such as 20:00–00:00 then 00:00–03:00 — which the public read then reports as "closes 00:00"; and **Unpublish and save** does not run the editor's error gate, so it can persist touching periods while unpublishing (no public serving effect while unpublished; Publish stays blocked by `hours_touching`). A direct RPC caller (manager+) or a legacy value can also hold touching windows, and the READ-001 SQL is unchanged by that ticket. Owned by [API_CONTRACT](API_CONTRACT.md) §4.42.1–§4.42.2. | Claude Code proposes / Human decides | Blocks a future READ-001 follow-up only (raised STOREFRONT-PUBLISH-001 final correction pass, 2026-09-25) | **Open** | **ASSUMPTION:** the weekly Dashboard check is enough for v1 and the date-exception and Unpublish-and-save residuals are accepted; a later Work ID either refuses touching windows in the validator (with a data check first) or merges contiguous current windows in `app.storefront_service_window` (which also covers exception boundaries), and gates Unpublish and save on the editor's errors. |

---

## Lifecycle and cross-references

- **Closing a question:** record a **DECISION D-xxx** in [DECISIONS.md](DECISIONS.md), then update the row here to `Status: Resolved → D-xxx` and add a dated note. Documents that cited the `Q-xxx` should then cite the new `D-xxx`.
- **Adding a question:** allocate the next free ID (**Q-025+**; Q-018–Q-024 are already allocated), fill every column, and note the originating document. Never renumber existing IDs.
- **Markers:** This register uses **OPEN QUESTION** (every row), **ASSUMPTION** (non-binding leanings), **DEFERRED** (Q-011, Q-016, and contingent items), **DECISION** (cited decisions, frozen only after review and approval), and **RISK** (cited register IDs). The **Status** field may also be **Accepted Open** per **DECISION D-027** (open but non-blocking for the M0B milestone under the four conditions above). Do not hide unresolved issues as silent assumptions.

### Related documents
- [DECISIONS.md](DECISIONS.md) — decisions register (`D-xxx`; frozen only after review and approval).
- [MONEY_AND_TAX_SPEC.md](MONEY_AND_TAX_SPEC.md) — money/tax/receipt rules (Q-002, Q-004, Q-007, Q-011, Q-012).
- [SECURITY_AND_THREAT_MODEL.md](SECURITY_AND_THREAT_MODEL.md) — auth/RLS/isolation (Q-008, Q-009, Q-017).
- [OFFLINE_SYNC_SPEC.md](OFFLINE_SYNC_SPEC.md) — sync/conflict/offline window (Q-009, Q-010, Q-014, Q-018–Q-024).
- [PRINTERS_AND_HARDWARE_SPEC.md](PRINTERS_AND_HARDWARE_SPEC.md) — printing/hardware (Q-006, Q-015).
- [OPERATIONS_AND_RECOVERY.md](OPERATIONS_AND_RECOVERY.md) — retention/backup/DR (Q-005, Q-013).
- [PILOT_PLAN.md](PILOT_PLAN.md) — pilot hardware and setup (Q-006, Q-015).
- [PROJECT_PLAN.md](PROJECT_PLAN.md) — milestone timing for blocking references.
- [MVP_SCOPE.md](MVP_SCOPE.md) — what is in/out of MVP (Q-016, Q-017).
