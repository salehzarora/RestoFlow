{
  "name": "bizbot-site",
  "version": "1.0.0",
  "private": true,
  "description": "BIZBOT | بِزبط — public marketing website (bizbot.systems). Static HTML per locale + one Vercel function for the demo-request form.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
